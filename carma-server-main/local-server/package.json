{
  "name": "carma-local-server",
  "version": "1.0.0",
  "description": "Local development server for CARMA app — mirrors Nave's FastAPI server on port 3000",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "localtunnel": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
